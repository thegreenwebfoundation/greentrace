import { expect } from 'chai';

import PageCheck from "./greentrace-service.js/index.js"

describe("GreenTrace", () => {
  describe("PageCheck", () => {
    it("fetches html content from a page")
    it("parses out href links from the dom")
    it("pulls the domain from the hrefs")
    it("builds a collection of domains from hrefs")
  })

  // the actual building of traceroutes, once we have a list of domains is handled by the greentrace-lib

})
